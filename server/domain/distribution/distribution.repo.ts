/**
 * DISTRIBUTION — repo: satu-satunya tempat query Drizzle untuk tabel rekap
 * distribusi laba (AD-5; modul ini read-only atas proyeksi ledger & poin
 * contribution). TODO(Epic 5): snapshot imutabel `numeric` string (AD-10).
 */
export {}
