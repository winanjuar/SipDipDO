/**
 * RKAP — fase & Capital Item (FR-23). Fulfillment dihitung dari alokasi
 * plotting ledger (derived, AD-2); sisa ruang = Σ Final Requirement −
 * Fulfillment transaksi EFEKTIF saja (pesanan antrai tidak mereservasi).
 *
 * TODO(Epic 2): tabel rkap_phases/capital_items; penyesuaian manual COO &
 * rebalancing mengikuti urutan lock global; ambang kap Big/Medium/Small =
 * data konfigurasi, bukan konstanta kode.
 */
export {}
