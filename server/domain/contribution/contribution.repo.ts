/**
 * CONTRIBUTION — repo: satu-satunya tempat query Drizzle untuk
 * `contribution_items`, `contribution_entries`, `contribution_periods` (AD-5).
 * TODO(Epic 5): poin integer; ratio `numeric(9,6)` string (AD-10).
 */
export {}
