/**
 * LEDGER — repo: satu-satunya tempat query Drizzle untuk `ledger_transactions`
 * dan `positions` (AD-5). TODO(Epic 3): uang `numeric(18,2)` string (AD-10);
 * tidak ada jalur UPDATE/DELETE untuk ledger (append-only, AD-1).
 */
export {}
