/**
 * AUDIT — repo: satu-satunya tempat query Drizzle untuk `audit_logs` (AD-5).
 * TODO(Story 1.3): insert-only; SELECT untuk tampilan COO; tanpa UPDATE/DELETE
 * di lapisan aplikasi (diperkuat DB grants).
 */
export {}
