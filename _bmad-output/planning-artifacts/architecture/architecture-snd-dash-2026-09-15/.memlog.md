---
scope: Sip & Dip Ownership Dashboard Phase 1 (PRD snd-dash 2026-08-14)
purpose: build-substrate + deck HTML untuk diskusi tim
altitude: initiative (menjaga features)
updated: 2026-09-15T17:07
---

- (event) Run activated: coaching path; deliverable spine (build-substrate) + deck HTML
- (constraint) Autentikasi seluruh akun via Google OAuth, dicocokkan lewat email saat migrasi (FR-22)
- (constraint) MFA OTP via email hanya untuk aksi transaksional COO: konfirmasi pesanan & input langsung (FR-3); owner tanpa MFA Phase 1
- (constraint) Pembayaran di luar sistem — sistem hanya mencatat tanggal + metode transfer, tanpa upload bukti (FR-20/FR-21, Non-Goals §5)
- (constraint) NFR presisi: seluruh perhitungan pecahan 2 desimal, pembulatan half-up (NFR §4.8)
- (constraint) Audit trail permanen append-only, tidak dapat diubah/dihapus, hanya terlihat COO (FR-12)
- (constraint) 3 tingkat akses: Calon Owner (halaman personal), Owner pemegang saham (transparansi penuh), COO (aksi transaksional + CMS); matriks keterbukaan §4.8; terbuka otomatis setelah Pembelian Pertama efektif
- (constraint) Non-goals: aplikasi native, payment gateway, akuntansi cafe, voting terautomasi, notifikasi ke semua owner (§5)
- (constraint) Bukti Transaksi per transaksi efektif via email, dapat diunduh ulang, format Template Konfirmasi Pembelian Saham v3 (FR-11)
- (constraint) Email adalah kanal keluar sistem satu-satunya: OTP MFA, Bukti Transaksi, notifikasi pesanan baru ke COO, pengingat pendaftar (FR-3/11/19/22)
- (constraint) Harga Terkunci saat submit; re-validasi memakai kondisi terkini saat konfirmasi; transaksi hanya efektif via COO (FR-1/FR-20)
- (constraint) Skala: internal tool 22 owner eksisting, pertumbuhan ~40; web app responsif dipakai dari browser HP (§6.1, UJ-1/UJ-2/UJ-6)
- (constraint) Migrasi data historis Google Sheets + Google Form; Grand Total hasil migrasi wajib cocok: Quantity 3.622, Shares 5.187, Ceil 14.980 (FR-14)
- (constraint) Glossary PRD wajib dipakai verbatim oleh workflow turunan; term metrik berbahasa Inggris (Quantity, Shares, Ceil, Strength, Portion, Contribution, Actual, RTL, dst.) (§3)
- (decision) Paradigma: modular monolith server-rendered + pulau interaktivitas (pilihan user: A). Satu deployable, HTML-first; JS berat hanya di pulau pratinjau pesanan & chart dashboard. Mencegah: split SPA/API dua codebase + drift kontrak data, microservices overkill untuk 22-40 pengguna
- (decision) Inti domain = LEDGER: tabel transaksi efektif append-only + audit log append-only; seluruh tampilan (tabel kepemilikan FR-4, chart FR-5/FR-18, Portion, Strength, RTL, Actual) adalah proyeksi baca turunan dari ledger. Mencegah: dua jalur mutasi state kepemilikan yang bisa berbeda hitungan; mutasi historis yang merusak audit trail FR-12 dan rekonsiliasi Bukti Transaksi
- (decision) Stack: Nuxt 4 (Vue 3 + TypeScript) full-stack — server-rendered via Nitro, satu repo satu deployable (pilihan user: nyaman TS/Vue). Mencegah: repo frontend/backend terpisah, drift kontrak data
- (decision) Database: PostgreSQL 18 — seluruh nilai uang & metrik pecahan disimpan sebagai numeric/decimal (bukan float), fondasi NFR half-up 2 desimal
- (version) Nuxt 4.x terverifikasi web Sep 2026 (nuxt.com)
- (version) PostgreSQL 18.6 terverifikasi web (postgresql.org, rilis Aug 2026; PG 19 masih beta)
- (version) NuxtAuth (sidebase) 1.3.1 terverifikasi web — Auth.js provider Google OAuth, middleware perlindungan route, API routes otomatis; cocok Nuxt 3+/4
- (question) ORM: Prisma 8 vs Drizzle — fork binary, menunggu konfirmasi user
- (question) Operational envelope: deployment target (VPS Docker self-host vs PaaS), mekanisme scheduler expiry 7 hari FR-19 + pengingat FR-22 — dibahas di coaching 4
- (decision) Deployment: Nuxt full-stack monolith di Vercel + Supabase sebagai managed PostgreSQL (region Singapore); FE/BE split Golang DITOLAK user setuju — dua bahasa, kontrak & auth ganda, dua pipeline tanpa imbalan skala
- (decision) Scheduler: Vercel Cron Jobs memanggil API route terproteksi; presisi harian cukup untuk expiry pesanan hari-7 (FR-19) dan pengingat/kedaluwarsa pendaftar (FR-22)
- (decision) Email keluar (OTP MFA FR-3, Bukti Transaksi FR-11, notifikasi pesanan COO FR-19, pengingat FR-22) via Resend/SMTP dari Nuxt server routes
- (decision) Bukti Transaksi PDF: @react-pdf/renderer di serverless; unduh ulang = regenerasi deterministik dari data ledger — single source of truth, tanpa file storage drift
- (decision) ORM = Drizzle (pilihan user) + postgres.js lewat Supabase connection pooler
- (event) Coaching 3 selesai: 10 modul + 5 aturan dependensi disetujui implisit (tanpa keberatan user); question ORM & deployment terjawab
- (version) Drizzle ORM terverifikasi web (orm.drizzle.team): stabil v0.45.x + drizzle-kit v0.31.x; v1.0 masih beta (beta.18 Mar 2026) — pin ke stabil
- (decision) Koreksi versi PG: Supabase hosted menjalankan PostgreSQL 17 (dukungan PG 14 berakhir 2026-07-01; image self-host default PG 17 sejak 2026-06-17; PG 18 belum tersedia hosted — terverifikasi changelog supabase.com Sep 2026). Keputusan 'PostgreSQL 18' direvisi menjadi 'PostgreSQL 17 via Supabase'; tipe numeric tetap, tanpa dampak ke NFR half-up
- (decision) Finalisasi transaksi atomik (FR-20/FR-21): SATU transaksi DB = append ledger + ubah status pesanan + plotting alokasi Capital Item + penyesuaian instant Final Requirement + entry audit; row-lock fase RKAP saat konfirmasi, first-confirm wins (kasus race PRD)
- (decision) Audit trail (FR-12): entry audit ditulis atomik dalam transaksi DB yang sama dengan aksinya — tidak pernah async/tangguh; append-only enforced DB grants; read khusus COO
- (decision) Side effect email (bukti/OTP/notifikasi) dikirim after-commit — kegagalan kirim tidak membatalkan transaksi efektif, tercatat untuk retry; bukti tetap dapat diunduh ulang (FR-11)
- (decision) Kepemilikan tabel mutlak per modul: posisi hanya ditulis LEDGER, Profile hanya IDENTITAS, harga hanya HARGA — tak ada modul menulis tabel modul lain
- (decision) OTP MFA (FR-3): disimpan hashed, single-use, TTL menit
- (decision) Proyeksi posisi = TABEL MATERIALIZED (pilihan user B, lean coach A tidak diikuti) dengan guardrail: update posisi HANYA boleh terjadi di dalam transaksi append ledger (kopling atomik) sehingga drift struktural mustahil; rekomputasi penuh dari ledger disediakan sebagai alat rekonsiliasi/audit berkala (jaring pengaman SM-1)
- (decision) Rumus domain tunggal: hitungan pembobotan (Ceil/Shares/Strength/RTL/gerbang RKAP) hidup di SATU modul TS bersama (shared/domain) yang dipakai server route DAN island pratinjau FR-1 — paritas pratinjau-server dijamin by construction
- (event) Coaching selesai (4/4). Mulai finalize: distill spine
- (event) Reconcile PRD (subagent independen): 6 temuan diterapkan semua — (high) ERD ledger↔buy_order jadi optional + catatan AD-2 pintu konfirmasi; (med) acceptance Grand Total migrasi masuk Testing; (low) outbox email retry, Template v3 eksplisit, rumus distribusi pro-rata masuk AD-6 + contoh 4.340.000, ambang cap = konfigurasi
- (event) Reviewer gate (3 lens paralel): rubric PASS-with-fixes; tech-currency DISCREPANCIES minor; adversarial 2 critical + 3 high. Semua fix diterapkan: AD-1 entry kompensasi; AD-2 CAS status + lock ordering + seluruh penulis RKAP satu lock + Fulfillment derived + pembayaran di ledger_transactions; AD-3 aktor system + registry event; AD-4 tabel biasa bukan MATERIALIZED VIEW; AD-5 outbox in-tx + edge lifecycle; AD-6 fungsi perakitan kanonik; AD-7 referral mekanis vs manusia; AD-9 backup/PITR + drill + batas hari Asia/Jakarta; AD-10 snapshot rekap imutabel + baris penyesuaian; AD-11 BARU siklus hidup owner satu penulis + definisi Pembelian Pertama efektif; stack: Drizzle v1.0-GA-at-scaffold, NuxtAuth smoke-test wajib, react-pdf note, postgres.js row, Node >=22; konvensi: kalender-hari Asia/Jakarta, migrasi price backfill via API HARGA, penolakan submit tanpa baris; diagram deployment dibenahi. Re-lint: 0 temuan
- (event) spine finalized — status final; deliverable kedua ARCHITECTURE-DECK.html (9 slide interaktif, navigasi keyboard) selesai; polish typo CSS+prosa
- (decision) Bentuk internal modul domain diseragamkan (seed): index.ts satu-satunya pintu impor lintas modul, *.service.ts kasus penggunaan, *.repo.ts satu-satunya tempat Drizzle utk tabel modul, events.ts opsional — mencegah tiap tim merancang struktur folder modulnya sendiri
- (event) Permintaan user: tambah Do/Don't — ditambahkan section 'Quick Reference — Do / Don't' (11 baris, tiap baris menunjuk AD/konvensi sumber; bukan keputusan baru). pages/ tipis + components/islands/ + composables/ diperjelas di tree. Catatan: konvensi repo lebih luas (lint, commit style) ditunda ke bmad-project-context saat kode ada
- (event) Permintaan user (update run): akomodasi PWA + jaminan platform web di laptop; resume memlog, intent update spine final
- (decision) PWA level A: installable dengan data selalu daring (pilihan user; B baca-luring dan C tanpa-PWA ditolak). Service worker hanya cache aset statis + cangkang; seluruh data domain diambil daring. Mencegah: cabang basis kode mobile vs desktop, angka domain usang tersaji dari cache, antrean tulis luring yang menjadi jalur mutasi kedua di luar pintu finalisasi AD-2
- (version) @vite-pwa/nuxt 1.1.1 terverifikasi web Sep 2026 (nuxt.com/modules/vite-pwa-nuxt + GitHub releases; rilis Feb 2026) — kompatibel Nuxt 4
- (decision) Invariant platform diratifikasi dari UX: satu basis kode responsif dua postur (mobile <lg dominan owner, desktop >=lg alur berat COO), tidak ada kapabilitas khusus satu postur; PWA = peningkatan bertahap di atas SSR. Mencegah: dua tim membangun varian UI terpisah per perangkat
- (event) Sinkron kedaluwarsa saat distill ulang: run UX Sep 2026 ternyata sudah final — baris Deferred 'UX docs belum dibuat' dikoreksi jadi merujuk DESIGN.md/EXPERIENCE.md; pilihan library chart tetap ditunda ke saat scaffold; sources frontmatter + EXPERIENCE.md (baris offline) disinkronkan dua arah
- (event) Gerbang review update (3 lens paralel): adversarial HOLES (1 critical U1 cangkang tak terdefinisi utk SSR, 3 high U2 cache HTTP/edge lolos dari invariant, U3 siklus update SW vs dialog MFA, U6 status owner tanpa CAS; 4 medium U4 postur manifest, U5 kepemilikan UX offline, U7 propagasi tx-handle, U8 otp_codes tanpa pemilik); rubric PASS-with-fixes (mermaid label rusak - regresi update, cangkang, Deferred kedaluwarsa, smoke-test parity); tech-currency masih berjalan
- (decision) Fix gerbang diterapkan: AD-12 dipertegas (definisi cangkang = aset fingerprint + manifest + ikon + /offline saja; dokumen SSR & payload Nuxt = data domain, tanpa precache/runtime-cache/navigateFallback selain /offline; no-store utk dokumen & /api/**; larangan swr/isr rute domain; registerType prompt tanpa skipWaiting, prompt update tak boleh di atas dialog transaksional; installed = viewport bukan postur, display standalone tanpa orientation, tanpa keying display-mode); AD-2 urutan lock diperpanjang (pesanan - fase RKAP - owner); AD-11 transisi status owner wajib CAS + rekap re-validasi shares=0 in-tx; AD-5 otp_codes milik IDENTITAS (requestOtp/verifyOtp single-use CAS); konvensi tx-handle lintas modul (hanya service teratas buka transaksi); mermaid deployment dikutip; @vite-pwa/nuxt wajib smoke-test Nuxt 4
- (event) Run dijeda oleh user sebelum lipatan tech-currency: deck sudah disegarkan, fix struktural + lint 0 temuan; sisa saat resume: lipatan temuan tech-currency (Stack wording saja) + event penutupan. User beralih setup repo git GitHub
- (event) Validate intent dijalankan user: lint 0 temuan; rubric PASS-with-fixes (1H: substrate UI shadcn/Vue; 1M: lifecycle pendaftaran; 4L); tech-currency MINOR-DISCREPANCIES (1M klaim vite-pwa/nuxt; 3L Node 24/NuxtAuth fallback/pin Drizzle eksak); adversarial HOLES-FOUND 14 pasangan divergen (1C: AD-2 penarikan kontradiktif; 7H: OTP binding, migrasi outbox, titik potong Bukti, lock ladder global, kawat uang string, derivasi status owner, poin lintas cut-off; 5M; 1L). Laporan: validation-report.html/.md. Spine TIDAK diubah (intent validate); usulan teks AD lengkap di review-adversarial-v2.md.
- (decision) AD-2 diperkuat (v2-F1 critical): penarikan = transisi tersimpan sendiri — kolom buy_orders.withdrawn_at timestamptz (BUKAN status), di-set dalam SATU tx + audit via CAS WHERE status='menunggu_konfirmasi' AND withdrawn_at IS NULL. SETIAP guard status pesanan (konfirmasi, cron expiry) wajib memuat AND withdrawn_at IS NULL. Definisi kanonik pesanan-pending = status='menunggu_konfirmasi' AND withdrawn_at IS NULL — dipakai pratinjau, validasi submit, re-validasi, cron. Kalimat lama 'penarikan juga compare-and-set pada status' dihapus (kontradiktif).
- (decision) AD-2/AD-11 (v2-F5 high): urutan lock diperluas jadi GLOBAL seluruh keluarga transaksi penulis: buy_orders -> rkap_phases -> positions -> owners -> contribution_periods -> distribution. Rekap DISTRIBUSI membaca posisi pada snapshot konsisten (REPEATABLE READ/atau lock urutan sama) sehingga SigmaPortion snapshot konsisten sebelum dibekukan; transisi IDENTITAS digerakkan event mengikuti urutan sama. Mencegah deadlock AB-BA finalisasi-vs-rekap dan rekap imutabel dengan Sigma != 100%.
- (decision) AD-6 (v2-F9 medium + F11 medium): enumerasi rumus diperluas — Portion dan seluruh persentase turunan chart termasuk shared/domain; island chart menerima angka display siap-format dari payload atau mengimpor fungsi shared, tanpa aritmetika rasio lokal. Fungsi perakitan input kanonik kini per-gerbang: Strength (posisi terkini + seluruh pesanan-pending kanonik), ruang RKAP (= Sigma Final Requirement - Fulfillment transaksi EFEKTIF saja; pesanan antrai tidak pernah mereservasi ruang, milik owner mana pun), sisa batas penyesuaian (Sigma penyesuaian fase), 'harga 1 saham berjalan' = harga berjalan momen finalisasi. Pratinjau, validasi submit, re-validasi memanggil fungsi assembly yang sama.
- (decision) AD-7 (v2-F14 low): tepat SATU baris price_periods per (jenis harga, tanggal efektif) — unique constraint; koreksi harga pada tanggal efektif sama = mengubah baris berjalan + audit, bukan baris kedua; resolusi 'harga berjalan pada tanggal X' selalu tepat satu baris (mencegah pratinjau vs snapshot beda harga).
- (decision) AD-8 (v2-F2 high + F10 + F13 + L-4 + F7 parsial): himpunan TERTUTUP aksi ber-MFA Phase 1 = konfirmasi (FR-20) + input langsung (FR-21); eksplisit BUKAN: penyesuaian RKAP, penetapan harga, cut-off, rebalancing, pergantian COO — endpoint & UI wajib mengacu himpunan sama, mengubah himpunan = mengubah AD ini. OTP terikat tepat satu instance aksi: otp_codes menyimpan {action_type, target_ref}; verifikasi DAN konsumsi terjadi DALAM transaksi finalisasi via API IDENTITAS yang menerima tx; rollback re-validasi mengembalikan konsumsi (retry tanpa email baru); attempt-limit + resend-cooldown (60 dtk) dienforce server-side. Kewenangan COO diautoritaskan DALAM tx finalisasi (cek coo_tenures berlaku pada now()); aktor audit = pejabat saat commit. Predikat aksesPenuh/perluReferral/pilihanReferral = fungsi kanonik tunggal IDENTITAS atas status siklus hidup (AD-11), TIDAK PERNAH diturunkan dari positions.shares live.
- (decision) AD-10 (v2-F6 high + rubric L-1 + F8 high): kontrak kawat — seluruh nilai uang & ratio melewati batas API/SSR sebagai string desimal berskala tetap; SATU pasangan parse/serialize diizinkan di shared/domain; Number()/parseFloat() atas nilai uang/ratio dilarang di semua lapisan termasuk display (+ baris Don't). Mekanisme aritmetika tunggal: decimal library (pin saat scaffold) — campuran idiom integer-sen dilarang. Rekap RUPS: menjumlahkan poin dari SEMUA periode ter-finalisasi-belum-tertunaikan; 'ditandai selesai' (cut-off FR-10) != 'ditunaikan' (rekap FR-16); penandaan tertunaikan atomik dengan penyimpanan rekap.
- (decision) AD-11 (v2 rubric M-1 + F7 high): enum siklus hidup owner/pendaftaran dipinkan: diajukan / terverifikasi / ditolak / kedaluwarsa / keluar (+ first_effective_at timestamptz, di-set HANYA oleh event Pembelian Pertama efektif). Hanya IDENTITAS menulis; transisi = compare-and-set atas status sebelumnya dalam SATU tx DB (verifikasi COO vs cron kedaluwarsa vs kelengkapan Profile — race kelas AD-2); kedaluwarsa pendaftaran = status tersimpan yang di-set cron (7 hari per UX), re-daftar diperbolehkan; event expiry masuk audit. Owner Keluar yang belum pernah membeli termasuk cakupan perluReferral & pilihanReferral.
- (decision) AD-12 (v2-F12 medium): kontrak kesegaran runtime — permukaan bernilai domain wajib salah satu: (a) refetch penuh pada visibilitychange/focus, atau (b) penanda waktu data ('per …') yang diperbarui bersama payload. Tabel dan island pada halaman yang sama mengonsumsi SATU payload bersama (SSR-embedded atau satu fetch) — tidak ada fetch domain independen per island pada render awal.
- (decision) Stack (v2 rubric H-1 high): substrate komponen UI = shadcn-vue (primitif reka-ui) + Tailwind — implementasi Vue dari kontrak shadcn DESIGN.md; WAJIB smoke-test paritas komponen kontrak (Dialog/Sheet/Tooltip/Drawer/Input-OTP/Toast) saat scaffold; catatan implementasi disinkronkan dua arah ke DESIGN.md & EXPERIENCE.md.
- (version) Stack pins dikoreksi (v2 tech-currency): @vite-pwa/nuxt 1.1.1 — klaim 'kompatibel Nuxt 4 terverifikasi' DICABUNK, kenyataannya vendor Nuxt-3-only (@nuxt/kit ^3.9.0, rilis terakhir Feb 2026, Nuxt 4.5 kini Vite 8/Rolldown); gerbang go/no-go = smoke-test BUILD + install + prompt di Nuxt 4.5; fallback: vite-plugin-pwa langsung / SW kustom direviu terhadap AD-12. Node: 24 LTS (24.21.x) untuk dev/CI/Vercel; engines floor >=22.19.0 per Nuxt 4.5.2 (Node 22 security-only). Drizzle: pin eksak drizzle-orm@0.45.2 + drizzle-kit@0.31.10 (v1.0 di rc.5 bukan GA; cek dist-tag saat scaffold, bila GA ambil + ulangi smoke migrasi). postgres.js pin 3.4.9. NuxtAuth 1.3.1 fallback dinamai: nuxt-auth-utils / OAuth manual (tetap memenuhi AD-8).
- (decision) Konvensi (v2-F3 + F4 high): Bukti Transaksi = fungsi murni state ledger PADA TITIK POTONG transaksinya (posisi owner sesaat setelah transaksi tersebut); regenerasi — retry outbox maupun unduh ulang — selalu menghitung pada titik potong yang sama, tidak pernah dari posisi mutakhir. Migrasi: import memakai pintu finalisasi LEDGER lengkap (posisi, event Pembelian Pertama efektif, audit aktor system) NAMUN tanpa baris outbox email untuk transaksi berlabel migrasi — Bukti migrasi hanya regenerate-on-demand; daftar event diteruskan vs disupres dideklarasikan eksplisit di modul MIGRASI dan diuji pada acceptance migrasi.
- (decision) Rumah kecil (v2 rubric L-2 + L-3): sources frontmatter + DESIGN.md (kontrak visual). Deferred + dua baris: error tracking & alerting (dijatuhkan saat scaffold; retry outbox gagal wajib terlihat — bukan kegagalan senyap) dan SPF/DKIM + From-domain Resend (tugas scaffold; OTP = kanal MFA, deliverabilitas load-bearing).
- (event) Update run selesai (roll findings v2 -> spine): 12 keputusan diterapkan (AD-2 penarikan+lock ladder global, AD-5 OTP via tx, AD-6 Portion+assembly per-gerbang+momen evaluasi, AD-7 unique+value-snapshot harga, AD-8 himpunan MFA tertutup+kompensasi+OTP binding+counter always-commit+single-live-code+tenure in-tx, AD-10 kawat string+decimal-lib+saham poin beku cut-off, AD-11 enum owner/pendaftaran+CAS+re-daftar same-row+kandidat flip, AD-12 kesegaran runtime, Stack shadcn-vue+Node 24+Drizzle eksak+vite-pwa jujur+NuxtAuth fallback, konvensi Migrasi pintu lengkap tanpa gerbang tanpa outbox, Bukti titik potong, Deferred error-tracking+SPF/DKIM, sources+DESIGN.md, ERD kardinalitas). Gate ulang: lint 0; rubric v3 PASS-with-fixes (semua fix diterapkan: counter OTP, path sources, momen evaluasi, edge pendaftaran, ERD); tech-currency v3 13 CURRENT (rc.4 dikoreksi, advisory dicatat); adversarial v3 14/14 prior CLOSED + 7 baru (N1-N7) SEMUA diterapkan. Sinkron dua arah UX (shadcn-vue) dilakukan. Spine tetap status final.
